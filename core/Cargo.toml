[package]
name = "imvoxcore"
version = "0.0.1"
edition = "2024"
license = "MIT"

[lib]
name = "imvoxcore"
crate-type = ["cdylib", "rlib"]

[dependencies]
mlua = { version = "0.12.0", features = ["lua54", "vendored"] }
