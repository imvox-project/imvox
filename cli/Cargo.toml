[package]
name = "imvoxcli"
version = "0.0.1"
edition = "2024"
license = "MIT"

[dependencies]
imvoxloader = { path = "../loader", version = "0.0.1" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
